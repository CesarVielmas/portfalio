import { Type } from '@angular/core';

export interface standardCardParameters {
  id: string;
  radiusCard:string;
  backgroundColorCard:string;
  borderCard:string;
  boxShadowCard:string;
  flexDirectionCard:string;
}